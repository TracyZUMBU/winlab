/** Page liste des loteries — données et table à ajouter. */
export function LotteriesPage() {
  return (
    <section className="page-lotteries" aria-labelledby="lotteries-heading">
      <h2 id="lotteries-heading" className="page-lotteries__heading">
        Lotteries
      </h2>
      <p className="page-lotteries__placeholder">
        Placeholder — la liste des loteries sera branchée ici.
      </p>
    </section>
  );
}
