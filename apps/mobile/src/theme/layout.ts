export const layout = {
  /** Minimum touch target (accessibility). */
  minTouchTarget: 44,
  /** Content row height for top app bars (excluding status bar / safe area). */
  headerContentMinHeight: 44,
  /** Mission list thumbnail (matches ~96 logical px). */
  missionThumbnailSize: 96,
} as const;
