# Monorepo root — dependencies often hoisted here
node_modules/

# Vite / front builds
dist/

# OS / editor noise
.DS_Store

# Local secrets at repo root (if any)
.env
.env.*.local

# Cursor local MCP config
/.cursor/mcp.json
